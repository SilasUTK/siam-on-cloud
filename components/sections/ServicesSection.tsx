import { Container, SectionHeading } from "@/components/ui";

export function ServicesSection() {
  return (
    <section id="services" className="py-20">
      <Container>
        <SectionHeading title="Services" subtitle="Services section placeholder." />
      </Container>
    </section>
  );
}
